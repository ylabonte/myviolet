{
  "name": "myviolet",
  "version": "0.1.1",
  "private": true,
  "description": "Version-source shim for changesets/action. The actual Python package metadata lives in pyproject.toml; this file exists only so the changesets CLI has a version field to bump. `private: true` prevents accidental `npm publish`.",
  "repository": "https://github.com/ylabonte/myviolet"
}
